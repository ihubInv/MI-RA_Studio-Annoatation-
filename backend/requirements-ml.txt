# ML inference stack (YOLO detection). Install on top of requirements-core.txt:
#   pip install -r requirements-ml.txt
#
# First run downloads pretrained weights (~6–22 MB per model).

-r requirements-core.txt

numpy==1.26.4
opencv-python-headless==4.10.0.84
torch==2.4.1
torchvision==0.19.1
ultralytics>=8.3.0
