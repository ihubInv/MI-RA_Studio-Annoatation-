# Annotation engines — moved

The empty `src/annotation/{image,video,...}` folders were a blueprint scaffold.

**Live image code is here:** `frontend/src/modules/image/`  
(panels, tools, canvas, studio)

Shared undo engine: `frontend/src/platform/annotation/AnnotationEngine.ts`

Future modalities: `frontend/src/modules/{video,audio,lidar,text,...}`
